name: validate-repository-assurance

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install validation dependencies
        run: python -m pip install jsonschema pyyaml
      - name: Run canonical repository validation
        run: make validate
      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tga-validation-evidence
          path: artifacts/validation/latest.json
          if-no-files-found: error
